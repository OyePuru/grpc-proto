name: Generate Stubs

on:
  push:
    paths:
      - 'proto/**'

jobs:
  generate-stubs:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: '1.22.0'

    - name: Install protoc
      run: |
        sudo apt-get update
        sudo apt-get install -y protobuf-compiler

    - name: Install protoc-gen-go
      run: |
        go install google.golang.org/protobuf/cmd/protoc-gen-go@latest

    - name: Install protoc-gen-go-grpc
      run: |
        go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest

    - name: Install protoc-gen-grpc-gateway
      run: |
        go install github.com/grpc-ecosystem/grpc-gateway/v2/protoc-gen-grpc-gateway@latest

    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install Node dependencies
      run: npm install

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install grpcio-tools
        pip install mypy-protobuf

      
    - name: Generate stubs
      run: |
        make generate-stubs-go
        make generate-stubs-ts
        make generate-stubs-python
    

    # - name: Push Golang Generations
    #   uses: cpina/github-action-push-to-another-repository@target-branch
    #   env:
    #     API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
    #   with:
    #     source-directory: ./gen/go
    #     destination-github-username: 'CrossBorder-Co'
    #     destination-repository-name: 'sales-marker-gen-go'
    #     user-email: 'aman.jain@cross-border.jp'
    #     commit-message: Generate Golang Code
    #     target-branch: main
    #     target-directory: gen/go

    # - name: Push Typescript Generations
    #   uses: cpina/github-action-push-to-another-repository@target-branch
    #   env:
    #     API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
    #   with:
    #     source-directory: ./gen/ts
    #     destination-github-username: 'CrossBorder-Co'
    #     destination-repository-name: 'sales-marker-gen-ts'
    #     user-email: 'aman.jain@cross-border.jp'
    #     commit-message: Generated Typescript Code
    #     target-branch: main
    #     target-directory: gen/ts

    # - name: Push Python Generations
    #   uses: cpina/github-action-push-to-another-repository@target-branch
    #   env:
    #     API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }}
    #   with:
    #     source-directory: ./gen/python
    #     destination-github-username: 'CrossBorder-Co'
    #     destination-repository-name: 'sales-marker-gen-python'
    #     user-email: 'aman.jain@cross-border.jp'
    #     commit-message: Generate Python Code
    #     target-branch: main
    #     target-directory: gen/python
